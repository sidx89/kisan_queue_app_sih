PORT=5000
NODE_ENV=development

# MySQL Database (XAMPP Default)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=smart_procurement

# JWT Secret
JWT_SECRET=kisan_smart_procure_jwt_secret_2026_secure_key
JWT_EXPIRES_IN=7d

# Maintenance Mode
MAINTENANCE_MODE=false

# Socket.IO & Admin Panel
ADMIN_ORIGIN=http://localhost:3000
